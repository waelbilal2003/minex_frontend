name: flutter_app
description: A Flutter mobile application
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  
  # Firebase packages (LOCKED versions for stability)
  firebase_core: 3.6.0
  firebase_auth: 5.3.1
  
  # Other dependencies
  http: 1.5.0
  shared_preferences: 2.5.3
  firebase_messaging: 15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
